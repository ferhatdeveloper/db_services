# Hasura Metadata Example
databases:
  - name: exfin_postgres
    kind: postgres
    configuration:
      connection_info:
        database_url: postgres://postgres:exfin2024@localhost:5432/postgres
        pool_settings:
          max_connections: 50
          idle_timeout: 180
          retries: 1 